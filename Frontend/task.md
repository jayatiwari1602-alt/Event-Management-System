# Tasks: Backend Connection and Custom Popup System

- [x] Create `common.js` with glassmorphic style injection, custom modal functions, and auth helpers.
- [x] Update `register.html` & `register.js` to hook up custom modal and signup endpoint.
- [x] Update `login.html` & `login.js` to hook up custom modal and login endpoint.
- [x] Update `events.html` & `events.js` to dynamically fetch events from backend and handle live search.
- [x] Update `event-detail.html` & `event-detail.js` to dynamically load details for specific event.
- [x] Update `booking.html` & `booking.js` to prefill details and post booking registration.
- [x] Update `dashboard.html` & `dashboard.js` to load personal registrations and stats.
- [x] Update `Landing.html` navbar integration.
- [x] Run backend and manually verify functionality.
